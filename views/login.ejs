<%include ./partials/header%>
    <div class="ui grid center aligned">
        <div class="ten wide computer ten wide tablet sixteen wide mobile column">
            <form class="ui form segment" action="/login" method="POST">
                <h4 class="ui dividing header">User login</h4>
                <div class="two fields">
                    <div class="field">
                        <label>Username</label>
                        <input type="text" name="username" required="required">
                    </div>
                    <div class="field">
                        <label>Password</label>
                        <input type="password" name="password" required="required">
                    </div>
                </div>
                <a href="/register">You don't have an account?</a>
                <div class="ui divider"></div>
                <button class="ui button orange" tabindex="0">Login</button>
            </form>
        </div>
    </div>
<%include ./partials/footer%>